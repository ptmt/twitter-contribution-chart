{
  "name": "tcc",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "line-navigator": "^2.1.6",
    "papaparse": "^4.4.0",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-dropzone": "^4.2.11",
    "react-progress-bar-plus": "^1.3.1",
    "react-scripts": "1.1.4",
    "twitter-contributions-canvas": "0.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && pushd build && rm static/js/*.map && now --public && now alias && popd"
  },
  "devDependencies": {
    "flow-bin": "^0.73.0"
  }
}
